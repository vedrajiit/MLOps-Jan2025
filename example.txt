This is the new example.txt file. Below is the new content of the file,


In the X+1 branch, modify the content of the existing file.
In the X-1 branch, replace the content entirely.
Commit these changes, then take a screenshot of the output of the following command:
git log --graph --oneline and include it in your report.

I am modifying this file with below content :


In the X+1 branch, modify the content of the existing file.
In the X-1 branch, replace the content entirely.
Commit these changes, then take a screenshot of the output of the following command:
git log --graph --oneline and include it in your report.